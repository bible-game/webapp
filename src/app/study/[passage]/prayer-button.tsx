"use client";
import {Button} from "@heroui/react";

export default function PrayerButton() {

    return <Button onPress={() => alert("Button pressed")}>Prayer: Guided Prayer from Scripture</Button>;
}
